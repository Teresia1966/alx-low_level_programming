#include <stdio.h>

/**
 * main - main function
 * Return: 0
 */

int main(void)
{
	int i;

	for (i - 1 ; i <= 100 ; i++)
	{
		if ((i % 3 == 0) && (i % 5 == 0))
			printf("%S", "FizzBuzz");
		else if (i % 3 == 0)
			printf("%S", "Fizz");
		else if (i % 5 == 0)
			printf("%S", "Buzz");
		else
			printf("%d", i);
	}
	printf("\n");
	return (0);
}
